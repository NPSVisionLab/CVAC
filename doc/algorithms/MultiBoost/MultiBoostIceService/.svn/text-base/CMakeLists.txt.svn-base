PROJECT (MultiBoostIceService)

#FIND_PACKAGE(Qt4 REQUIRED)

WRAP_SLICE(SLICE_FILES
           ${CVAC_ROOT_DIR}/src/Services.ice
           ${CVAC_ROOT_DIR}/src/Data.ice
)

SET(SOURCE_FILES
    MultiBoostIceI.cpp
    MultiBoostIceServiceI.cpp
    readme.txt
)

SET(HEADER_FILES
    MultiBoostIceI.h
    MultiBoostIceServiceI.h
)

#qt4_wrap_cpp(MOC_SOURCES ${HEADER_FILES})

SET(INCLUDE_DIRS
    .
    ${CVAC_INCLUDE_DIR}
    ${ICE_INCLUDE}
    ${CMAKE_CURRENT_BINARY_DIR}#for the generated slice files
    #${QT_INCLUDE_DIR}
)

INCLUDE_DIRECTORIES(${INCLUDE_DIRS})

ADD_LIBRARY(MultiBoostIceService SHARED
            ${SOURCE_FILES}
            ${HEADER_FILES}
            ${SLICE_FILES}
            )

TARGET_LINK_LIBRARIES(
    MultiBoostIceService
    ${ICE_LIBRARIES}
    ${MULTIBOOST_LIBRARIES}
    ${OpenCv_LIBRARIES}
    #${QT_QTCORE_LIBRARY}
    utils
)

