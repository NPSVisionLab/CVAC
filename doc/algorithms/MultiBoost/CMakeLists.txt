FIND_PACKAGE(MultiBoost)

INCLUDE_DIRECTORIES( ${MULTIBOOST_INCLUDE})
ADD_SUBDIRECTORY(MultiBoostIceService)

IF (${MULTIBOOST_TRAINER_LIB})
  ADD_SUBDIRECTORY(MultiBoostTrainerService)
ENDIF ()

