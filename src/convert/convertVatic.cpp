#include <convert/convertVatic.h>
using namespace cvac;

LabelableList* cvac::convert(std::string filePath) {

	printf("Test: convert method.\n");

	LabelableList* returnList = NULL;
	return(returnList);
}